# Railway Configuration for SegurifAI x PAQ
# Uses Dockerfile for build

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Explicit start command ensures seed runs even if Docker CMD is cached
startCommand = "bash /app/startup.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
